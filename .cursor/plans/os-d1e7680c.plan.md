<!-- d1e7680c-cc20-47da-a63c-d4adaa1434c2 6b7c9bf8-385a-40be-9b4a-a52bd0fc5349 -->
# Phase 4.5-4.6: ブラウザ実装とタスクトレイ常駐・自動起動完全実装

## 実装方針

- **型定義**: すべての関数・構造体に明示的な型注釈、JSDoc/Rustdoc完全記載
- **警告0**: Clippy/ESLint警告を完全排除、`#[allow]`は最小限
- **ベストプラクティス**: Deep Research結果を反映、エラーハンドリング完全実装

## Phase 4.6: タスクトレイ常駐・自動起動（優先実装）

### 4.6.2: Windows自動起動設定拡張

**ファイル**: `codex-rs/tauri-gui/src-tauri/src/autostart.rs`

**実装内容**:

- `winreg`クレート0.55.0を使用（`Cargo.toml`に追加）
- `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`に登録
- `--tray`フラグでタスクトレイ常駐モード起動
- エラーハンドリング完全実装（`Result<T, String>`でTauriコマンド）

**コード例**:

```rust
use winreg::enums::*;
use winreg::RegKey;
use std::io;

pub fn set_autostart(enabled: bool) -> Result<(), String> {
    let hkcu = RegKey::predef(HKEY_CURRENT_USER);
    let path = r"Software\Microsoft\Windows\CurrentVersion\Run";
    let (key, _) = hkcu.create_subkey(path)
        .map_err(|e| format!("Failed to open registry key: {}", e))?;
    
    if enabled {
        let exe_path = std::env::current_exe()
            .map_err(|e| format!("Failed to get executable path: {}", e))?;
        let command = format!("\"{}\" --tray", exe_path.to_string_lossy());
        key.set_value("Codex", &command)
            .map_err(|e| format!("Failed to set registry value: {}", e))?;
    } else {
        key.delete_value("Codex")
            .map_err(|e| format!("Failed to delete registry value: {}", e))?;
    }
    Ok(())
}

#[tauri::command]
pub async fn is_autostart_enabled() -> Result<bool, String> {
    let hkcu = RegKey::predef(HKEY_CURRENT_USER);
    let path = r"Software\Microsoft\Windows\CurrentVersion\Run";
    let key = hkcu.open_subkey(path)
        .map_err(|e| format!("Failed to open registry key: {}", e))?;
    Ok(key.get_value::<String, _>("Codex").is_ok())
}
```

**依存関係追加**: `codex-rs/tauri-gui/src-tauri/Cargo.toml`

```toml
[dependencies]
winreg = "0.55"
```

### 4.6.1: タスクトレイ統合拡張

**ファイル**: `codex-rs/tauri-gui/src-tauri/src/tray.rs`

**実装内容**:

- セキュリティメニュー項目追加（マルウェア検知、パスワード管理）
- 仮想OSアクセスメニュー項目追加
- 自動起動ON/OFF切り替えメニュー項目追加
- 通知機能拡張（将来のセキュリティ機能統合を考慮）

**メニュー構成拡張**:

```
📊 Dashboardを開く
────────────────
✅ ファイル監視: ON
🔄 Codex Core: 起動中
────────────────
🔒 セキュリティ
   ├── マルウェア検知
   ├── パスワード管理
   └── 脆弱性スキャン
🖥️ 仮想OS
   ├── WSL2環境
   └── Docker環境
────────────────
⚙️ Settings
📖 Docs
🚀 自動起動: ON/OFF
❌ Quit
```

### 4.6.3: バックグラウンド実行

**新規ファイル**: `codex-rs/tauri-gui/src-tauri/src/background_tasks.rs`

**実装内容**:

- `tokio::time::interval`で定期実行（マルウェア監視、漏洩チェック）
- `tokio::spawn`で非同期タスク実行
- `Arc<AtomicBool>`でキャンセレーションフラグ管理
- エラーハンドリングとリトライロジック実装

**コード例**:

```rust
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Arc;
use tokio::time::{interval, Duration};
use tracing::{info, error};

pub struct BackgroundTaskManager {
    is_running: Arc<AtomicBool>,
}

impl BackgroundTaskManager {
    pub fn new() -> Self {
        Self {
            is_running: Arc::new(AtomicBool::new(false)),
        }
    }

    pub async fn start_malware_monitoring(&self) -> Result<(), String> {
        let is_running = Arc::clone(&self.is_running);
        is_running.store(true, Ordering::SeqCst);
        
        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(300)); // 5分間隔
            while is_running.load(Ordering::SeqCst) {
                interval.tick().await;
                // TODO: マルウェア監視実装（Phase 4.4で実装予定）
                info!("Malware monitoring check (placeholder)");
            }
        });
        Ok(())
    }
}
```

### 4.6.4: GUI統合（設定UI）

**新規ファイル**: `codex-rs/tauri-gui/src/components/system/SystemTraySettings.tsx`

**実装内容**:

- 自動起動設定UI（ON/OFF切り替え）
- タスクトレイ常駐設定
- 通知設定
- 型定義完全実装（TypeScript strict mode）

**型定義例**:

```typescript
interface SystemTraySettingsProps {
  onAutostartChange?: (enabled: boolean) => void;
  onTrayResidentChange?: (enabled: boolean) => void;
}

interface AutostartState {
  enabled: boolean;
  loading: boolean;
  error: string | null;
}
```

## Phase 4.5: インターネット接続・ブラウザ実装

### 4.5.1: 仮想OS環境内でのインターネット接続

**新規ファイル**: `codex-rs/core/src/virtualization/network.rs`

**実装内容**:

- WSL2/Dockerコンテナからのインターネット接続確認
- プロキシ設定管理（`/etc/environment`、`~/.docker/config.json`）
- ファイアウォールルール管理（Windows `netsh`、PowerShell `New-NetFirewallRule`）
- ポートフォワーディング（`netsh interface portproxy`）

**コード例**:

```rust
use anyhow::Result;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct NetworkConfig {
    pub proxy_enabled: bool,
    pub http_proxy: Option<String>,
    pub https_proxy: Option<String>,
    pub no_proxy: Option<Vec<String>>,
}

pub struct NetworkManager;

impl NetworkManager {
    pub async fn check_connection(environment: &str) -> Result<bool> {
        // WSL2/Docker環境での接続確認
        match environment {
            "wsl2" => {
                // wsl ping google.com -c 1
                Ok(true) // 実装
            }
            "docker" => {
                // docker exec container ping google.com -c 1
                Ok(true) // 実装
            }
            _ => Err(anyhow::anyhow!("Unknown environment")),
        }
    }
}
```

### 4.5.2: 組み込みブラウザ（マルウェア検知統合）

**新規ファイル**: `codex-rs/tauri-gui/src/components/virtual/VirtualOSBrowser.tsx`

**実装内容**:

- Tauri WebView API使用（`@tauri-apps/plugin-webview`）
- Content Security Policy (CSP)設定（`tauri.conf.json`）
- XSS保護（サニタイゼーション、DOMPurify統合）
- フィッシング検出（URL検証、ブラックリスト）
- ダウンロード前スキャン（マルウェア検知統合、将来実装）

**CSP設定例** (`tauri.conf.json`):

```json
{
  "tauri": {
    "security": {
      "csp": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; connect-src ipc: http://ipc.localhost https://*; img-src 'self' data: https://*;"
    }
  }
}
```

**型定義例**:

```typescript
interface VirtualOSBrowserProps {
  initialUrl?: string;
  onUrlChange?: (url: string) => void;
  onDownload?: (url: string) => Promise<boolean>; // マルウェアスキャン結果
}

interface BrowserSecurityState {
  isPhishing: boolean;
  isMalware: boolean;
  blockedUrls: string[];
}
```

**依存関係追加**: `codex-rs/tauri-gui/package.json`

```json
{
  "dependencies": {
    "@tauri-apps/plugin-webview": "^2.0.0",
    "dompurify": "^3.0.0"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.0"
  }
}
```

## 実装順序

1. **Phase 4.6.2**: Windows自動起動設定（`autostart.rs`拡張、`winreg`追加）
2. **Phase 4.6.1**: タスクトレイ統合拡張（`tray.rs`拡張、メニュー項目追加）
3. **Phase 4.6.3**: バックグラウンド実行（`background_tasks.rs`新規作成）
4. **Phase 4.6.4**: GUI統合（`SystemTraySettings.tsx`新規作成）
5. **Phase 4.5.1**: ネットワーク接続確認（`network.rs`新規作成）
6. **Phase 4.5.2**: 組み込みブラウザ実装（`VirtualOSBrowser.tsx`新規作成、CSP設定）

## 型定義・警告0の実装方針

### Rust

- すべての関数に明示的な戻り値型（`Result<T, E>`）
- `#[must_use]`を適切に使用
- `#[allow]`は最小限、理由をコメント記載
- Clippy警告を完全排除（`cargo clippy -- -D warnings`）

### TypeScript

- `strict: true`、`noImplicitAny: true`
- すべての関数・コンポーネントに型注釈
- JSDoc完全記載（`@param`、`@returns`、`@throws`）
- ESLint警告を完全排除

## 実装ファイル一覧

### バックエンド（Rust）

- `codex-rs/tauri-gui/src-tauri/src/autostart.rs` - 自動起動設定拡張
- `codex-rs/tauri-gui/src-tauri/src/tray.rs` - タスクトレイ拡張
- `codex-rs/tauri-gui/src-tauri/src/background_tasks.rs` - バックグラウンドタスク（新規）
- `codex-rs/core/src/virtualization/network.rs` - ネットワーク接続（新規）
- `codex-rs/tauri-gui/src-tauri/Cargo.toml` - 依存関係追加（`winreg`）

### フロントエンド（TypeScript/React）

- `codex-rs/tauri-gui/src/components/system/SystemTraySettings.tsx` - 設定UI（新規）
- `codex-rs/tauri-gui/src/components/virtual/VirtualOSBrowser.tsx` - ブラウザコンポーネント（新規）
- `codex-rs/tauri-gui/src/pages/Settings.tsx` - 設定ページ統合
- `codex-rs/tauri-gui/package.json` - 依存関係追加（`@tauri-apps/plugin-webview`、`dompurify`）
- `codex-rs/tauri-gui/src-tauri/tauri.conf.json` - CSP設定追加

## 注意事項

- Windows自動起動は`winreg`クレート0.55.0を使用
- タスクトレイメニューは既存の`tray.rs`を拡張（破壊的変更なし）
- バックグラウンドタスクは将来のセキュリティ機能拡張を考慮
- ブラウザ実装はTauri WebView APIを使用（CSP必須）
- すべての機能は型定義を厳格化し、警告0で実装

### To-dos

- [ ] Windows自動起動設定実装（autostart.rs拡張、winregクレート使用、型定義完全、警告0）
- [ ] タスクトレイメニュー拡張（セキュリティ・仮想OS・自動起動メニュー追加、型定義完全）
- [ ] バックグラウンドタスク実装（background_tasks.rs新規作成、Tokio async、型定義完全、警告0）
- [ ] システムトレイ設定UI実装（SystemTraySettings.tsx新規作成、型定義完全、strict mode）
- [ ] ネットワーク接続確認実装（network.rs新規作成、WSL2/Docker対応、プロキシ・ファイアウォール管理、型定義完全）
- [ ] 組み込みブラウザ実装（VirtualOSBrowser.tsx新規作成、CSP設定、XSS保護、フィッシング検出、型定義完全）
- [ ] 型定義・警告0の検証（Rust: cargo clippy -- -D warnings、TypeScript: tsc --noEmit、ESLint）