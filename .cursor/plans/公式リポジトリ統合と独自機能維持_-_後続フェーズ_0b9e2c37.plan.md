---
name: 公式リポジトリ統合と独自機能維持 - 後続フェーズ
overview: Phase 1-6完了後の後続タスク（依存関係の型エラー解消、セキュリティ監査、upstream/mainとのマージ準備）を実行する計画
todos: []
isProject: false
---

# 公式リポジトリ統合と独自機能維持 - 後続フェーズ

## 現状

Phase 1-6は完了済み。実装ログに記載されている「次のステップ」を実行する。

## Phase 7: 依存関係の型エラー解消

### 7.1 oauth2バージョン不一致の解消

**問題**:

- `codex-rs/core/Cargo.toml`: `oauth2 = "4.4"`
- `codex-rs/rmcp-client/Cargo.toml`: `oauth2 = "5"`

**対応**:

- ワークスペース全体でoauth2のバージョンを統一
- 最新バージョン（5.x）に統一するか、互換性を確認して適切なバージョンを選択

**確認ファイル**:

- `codex-rs/Cargo.toml` - ワークスペース依存関係
- `codex-rs/core/Cargo.toml` - core依存関係
- `codex-rs/rmcp-client/Cargo.toml` - rmcp-client依存関係
- `codex-rs/microsoft365/Cargo.toml` - microsoft365依存関係

### 7.2 chrono/schemars型エラーの確認と修正

**確認内容**:

- `cargo check`で型エラーを確認
- chrono、schemars関連の型エラーを特定
- 必要に応じて依存関係のバージョンを更新

**確認コマンド**:

```bash
cargo check --workspace 2>&1 | grep -E "(error|chrono|schemars)"
```

### 7.3 型エラーの修正

- 検出された型エラーを修正
- 依存関係のバージョン更新が必要な場合は、互換性を確認してから更新

## Phase 8: セキュリティ監査の実行

### 8.1 cargo-auditのインストールと実行

**手順**:

1. `cargo install cargo-audit`でcargo-auditをインストール
2. `cargo audit`でRust依存関係の脆弱性をスキャン
3. 検出された脆弱性を修正

**確認コマンド**:

```bash
cargo install cargo-audit
cargo audit
```

### 8.2 npm auditの実行

**手順**:

1. `npm i --package-lock-only`でlockfileを作成（存在しない場合）
2. `npm audit`でNode.js依存関係の脆弱性をスキャン
3. 検出された脆弱性を修正

**確認コマンド**:

```bash
cd gui  # または適切なディレクトリ
npm i --package-lock-only
npm audit
```

### 8.3 セキュリティ脆弱性の修正

- 検出された脆弱性を優先度順に修正
- 修正できない場合は、リスク評価を実施

## Phase 9: upstream/mainとのマージ準備

### 9.1 現在の変更のコミット

**手順**:

1. 変更されたファイルを確認
2. 適切なコミットメッセージでコミット
3. 必要に応じて複数のコミットに分割

**変更ファイル**（git statusより）:

- `codex-rs/cli/Cargo.toml`
- `codex-rs/cli/src/main.rs`
- `codex-rs/core/Cargo.toml`
- `codex-rs/core/src/agents/runtime.rs`
- `codex-rs/core/src/cowork_integration.rs`
- `codex-rs/core/src/git4d_accelerated.rs`
- `codex-rs/core/src/lib.rs`
- `codex-rs/core/src/orchestration/qc_merger.rs`
- `codex-rs/core/src/orchestration/worktree_manager.rs`
- `codex-rs/core/src/resource_monitor.rs`
- `codex-rs/core/src/vr_ar_integration.rs`
- `codex-rs/utils/git/src/errors.rs`
- `gui/src/lib/xr/webxr-manager.ts`
- `tests/playwright/gui-tests.spec.ts`

### 9.2 マージ戦略の決定

**オプション**:

1. **直接マージ**: `git merge upstream/main`を実行
2. **リベース**: `git rebase upstream/main`で履歴を整理
3. **段階的マージ**: feature branchで段階的にマージ

**推奨**: 大量のコンフリクトが予想されるため、feature branchで段階的にマージ

### 9.3 コンフリクト解決の準備

**準備事項**:

- バックアップブランチの作成
- コンフリクト解決の自動化スクリプト（必要に応じて）
- 各機能ごとのテスト計画

## Phase 10: 最終検証とドキュメント更新

### 10.1 コンパイル確認

- `cargo check --workspace`で型エラー0を確認
- `cargo clippy --all-targets --all-features -- -D warnings`で警告0を確認

### 10.2 テスト実行

- すべてのテストがパスすることを確認
- 独自機能の統合テストを実行

### 10.3 実装ログの更新

- `_docs/2026-01-25_公式リポジトリ統合と独自機能維持{main}.md`を更新
- Phase 7-10の実装内容を記録

## 実装順序

1. **Phase 7**: 依存関係の型エラー解消（必須）
2. **Phase 8**: セキュリティ監査の実行（必須）
3. **Phase 9**: upstream/mainとのマージ準備（必須）
4. **Phase 10**: 最終検証とドキュメント更新（推奨）

## リスクと対策

### リスク1: oauth2バージョン不一致による破壊的変更

**対策**:

- バージョン5.xの変更点を確認
- 必要に応じてコードを更新
- 段階的な移行を実施

### リスク2: セキュリティ脆弱性の大量検出

**対策**:

- 優先度順に修正
- 修正できない場合は、リスク評価を実施
- ドキュメント化

### リスク3: upstream/mainとのマージ時の大量コンフリクト

**対策**:

- feature branchで段階的にマージ
- バックアップブランチの作成
- 各機能ごとに独立してテスト

## 成功基準

1. ✅ 依存関係の型エラー0

2