# 引き継ぎメタプロンプト: Codex プロジェクト セマンティクス更新 v2.14.0

## 1. コンテキスト

**プロジェクト**: Codex - AIコーディングアシスタント (OpenAI Codex CLIの拡張フォーク)
**リポジトリ**: `c:\Users\downl\Desktop\codex-main`
**バージョン**: 2.14.0
**日時**: 2026-02-06
**ビルドシステム**: `ultra-fast-build-install.ps1` v1.3.0

---

## 2. 現在の状態

### 2.1 解決済み

- `windows-sandbox-rs/src/lib.rs`: Git conflict markers 解消済み
- `codex-api/src/endpoint/compact.rs`: Git conflict markers 解消済み
- `codex-windows-sandbox`: `cargo check` 成功

### 2.2 未解決のブロッカー (ビルド・コンパイルエラー)

以下のエラーが `check_exec_errors_final_2.txt` および最新のビルドログで確認されています。これらは最優先事項です。

#### **`mcp-server` (重大)**

- `external_mcp_manager.rs`:
  - `dirs` クレートの import エラー (`use dirs;` が解決できない)
  - `info!` マクロが見つからない (log クレートの import 漏れ)
- `external_mcp_tool_handler.rs`:
  - `OutgoingMessage::CallTool` バリアントが見つからない (定義不一致)
  - `ToolResponseContent` 未解決 import
- `external_mcp_tool.rs`:
  - `Tool` 構造体のフィールド (`input_schema` 等) が不足または不一致
- `message_processor.rs`:
  - `rmcp::model::CallToolResult` と `mcp_types::CallToolResult` の型不一致
  - `error_code` 未解決 import
- `webhook_tool_handler.rs`:
  - 型不一致: `Option<Vec<Value>>` vs `Option<Value>`

#### **`core`**

- `WireApi`: 定義ミスマッチ (期待されるフィールド/メソッドが不足)
- `ResponseItem`: フィールド不足

#### **`tui`**

- モジュール名の競合: `history_cell` が重複定義されている、またはパスが不正

#### **CI / GitHub Actions**

- 入力エラーおよびシークレットに関する警告が発生中

### 2.3 インフラ・ツール最適化 (Pending Tasks)

- **`SKILL.md` (Global)**: UTF-8 エンコーディングへの統一
- **`qc-optimizer`**: `SKILL.md` の YAML ヘッダー/フォーマット修正
- **`serena`**: `stdout` への JSON-RPC 出力の排他制御実装 (ログ混入防止)

---

## 3. 実装計画

### 3.1 Phase 1: コンパイルエラー完全解消 (最優先)

1. **`mcp-server` の修正**:
   - `Cargo.toml` に `dirs`, `log` が含まれているか確認し、適切に `use` を追加。
   - `external_mcp_tool_handler.rs` 等の型定義を最新の `codex-core` (または `codex-mcp`) に合わせる。
2. **`core` & `tui` の修正**:
   - `WireApi` 等の struct 定義を確認し、呼び出し側を修正。
3. **全ての git conflict markers を検出・解消**
   ```powershell
   Select-String -Path "c:\Users\downl\Desktop\codex-main\codex-rs\**\*.rs" -Pattern "<<<<<<< HEAD" -Recurse
   ```
4. **ワークスペース全体のビルド成功を確認**
   ```powershell
   cargo check --workspace --all-targets
   ```

### 3.2 Phase 2: Rust 2024 ベストプラクティス適用

- **Edition**: `edition = "2024"` (既に設定済み: `Cargo.toml` 71行目)
- **Lints**: `[workspace.lints.clippy]` の厳格なルール遵守 (286-322行目)
- **unsafe コードゼロ**: `unsafe` ブロックは最小限に
- **エラーハンドリング**: `thiserror` + `anyhow` パターン
- **型安全**: ジェネリクス、ライフタイム明示

### 3.3 Phase 3: React/Vite/Three.js (GUI) ベストプラクティス

- **ディレクトリ**: `c:\Users\downl\Desktop\codex-main\gui`
- **TypeScript**: `strict` モード、`any` 禁止
- **React**: 関数コンポーネント + Hooks
- **Three.js**: `@react-three/fiber` + `@react-three/drei` パターン
- **ESLint/Prettier**: 全警告解消

---

## 4. 品質基準 (必須達成)

| 項目              | 目標                                          |
| ----------------- | --------------------------------------------- |
| **型定義エラー**  | 0                                             |
| **Clippy 警告**   | 0 (`cargo clippy --workspace -- -D warnings`) |
| **ESLint 警告**   | 0 (`npm run lint` in `gui/`)                  |
| **未使用 import** | 0                                             |
| **ビルド成功**    | `cargo build --release --workspace` 成功      |

---

## 5. ソフトウェア工学ベストプラクティス

1. **1000行超のファイルは分割**: `core/src/codex.rs` (~253KB) 等、巨大ファイルはモジュール分割を検討
2. **関心の分離**: モジュール単位で責務を明確化
3. **テスト駆動**: 新規コードには単体テスト追加
4. **ドキュメント**: `///` コメントで pub API を文書化
5. **実装ログ**: 完了時に `_docs/yyyy-mm-dd_機能名.md` を作成 (必須)

---

## 6. 主要ファイル参照

- **Workspace設定**: `codex-rs/Cargo.toml`
- **実装計画**: `C:\Users\downl\.gemini\antigravity\brain\04e07aca-c27f-4e53-839c-462de1f2297e\implementation_plan.md`
- **タスクリスト**: `C:\Users\downl\.gemini\antigravity\brain\04e07aca-c27f-4e53-839c-462de1f2297e\task.md`
- **ビルドスクリプト**: `codex-rs/ultra-fast-build-install.ps1` (tqdm風プログレスバー実装済み)

---

## 7. コマンドリファレンス

```powershell
# Git conflict 検出
Select-String -Path "codex-rs\**\*.rs" -Pattern "<<<<<<< HEAD" -Recurse

# ワークスペースチェック
cargo check --workspace --all-targets

# Clippy 厳格チェック
cargo clippy --workspace --all-targets -- -D warnings

# 高速並列ビルド (12コア)
.\ultra-fast-build-install.ps1 -MaxParallelJobs 12

# GUI lint
cd gui; npm run lint
```

---

## 8. 次のアクション

1. **`mcp-server` のビルドエラー解消** (最優先)
2. `core`, `tui` 等の残存エラー解消
3. `grep` で残存 conflict markers を消去
4. `cargo check --workspace` 成功確認
5. `cargo clippy --workspace -- -D warnings` 警告0達成
6. `gui/` で `npm run lint` 警告0達成
7. 全バイナリビルド・インストール
8. `_docs/` ディレクトリに実装ログを作成

---

**戦略**: 型安全第一、ベストプラクティス遵守、完全統合を目指す
