# 引き継ぎメタプロンプト: Codex プロジェクト セマンティクス更新

## 1. コンテキスト

**プロジェクト**: Codex - AIコーディングアシスタント (OpenAI Codex CLIの拡張フォーク)
**リポジトリ**: `c:\Users\downl\Desktop\codex-main`
**バージョン**: 2.13.0
**日時**: 2026-02-06

---

## 2. 現在の状態

### 2.1 解決済み

- `windows-sandbox-rs/src/lib.rs`: Git conflict markers 解消済み
- `codex-api/src/endpoint/compact.rs`: Git conflict markers 解消済み
- `codex-windows-sandbox`: `cargo check` 成功

### 2.2 未解決のブロッカー

- `codex-exec`: 依存クレートのコンパイルエラー (詳細は `check_exec_errors_final_2.txt` 参照)
- Git conflict markers が残存している可能性のあるファイル:
  - `grep -r "<<<<<<< HEAD" codex-rs/` で確認すること

---

## 3. 実装計画

### 3.1 Phase 1: コンパイルエラー解消 (最優先)

1. **全ての git conflict markers を検出・解消**
   ```powershell
   Select-String -Path "c:\Users\downl\Desktop\codex-main\codex-rs\**\*.rs" -Pattern "<<<<<<< HEAD" -Recurse
   ```
2. **ワークスペース全体のビルド成功を確認**
   ```powershell
   cargo check --workspace --all-targets
   ```

### 3.2 Phase 2: Rust 2024 ベストプラクティス適用

- **Edition**: `edition = "2024"` (既に設定済み: `Cargo.toml` 71行目)
- **Lints**: `[workspace.lints.clippy]` の厳格なルール遵守 (286-322行目)
- **unsafe コードゼロ**: `unsafe` ブロックは最小限に
- **エラーハンドリング**: `thiserror` + `anyhow` パターン
- **型安全**: ジェネリクス、ライフタイム明示

### 3.3 Phase 3: React/Vite/Three.js (GUI) ベストプラクティス

- **ディレクトリ**: `c:\Users\downl\Desktop\codex-main\gui`
- **TypeScript**: `strict` モード、`any` 禁止
- **React**: 関数コンポーネント + Hooks
- **Three.js**: `@react-three/fiber` + `@react-three/drei` パターン
- **ESLint/Prettier**: 全警告解消

---

## 4. 品質基準 (必須達成)

| 項目              | 目標                                          |
| ----------------- | --------------------------------------------- |
| **型定義エラー**  | 0                                             |
| **Clippy 警告**   | 0 (`cargo clippy --workspace -- -D warnings`) |
| **ESLint 警告**   | 0 (`npm run lint` in `gui/`)                  |
| **未使用 import** | 0                                             |
| **ビルド成功**    | `cargo build --release --workspace` 成功      |

---

## 5. ソフトウェア工学ベストプラクティス

1. **1000行超のファイルは分割**: `core/src/codex.rs` (~253KB) 等
2. **関心の分離**: モジュール単位で責務を明確化
3. **テスト駆動**: 新規コードには単体テスト追加
4. **ドキュメント**: `///` コメントで pub API を文書化
5. **実装ログ**: 完了時に `_docs/yyyy-mm-dd_機能名.md` を作成

---

## 6. 主要ファイル参照

- **Workspace設定**: `codex-rs/Cargo.toml`
- **実装計画**: `C:\Users\downl\.gemini\antigravity\brain\04e07aca-c27f-4e53-839c-462de1f2297e\implementation_plan.md`
- **タスクリスト**: `C:\Users\downl\.gemini\antigravity\brain\04e07aca-c27f-4e53-839c-462de1f2297e\task.md`
- **ビルドスクリプト**: `codex-rs/ultra-fast-build-install.ps1` (tqdm風プログレスバー実装済み)

---

## 7. コマンドリファレンス

```powershell
# Git conflict 検出
Select-String -Path "codex-rs\**\*.rs" -Pattern "<<<<<<< HEAD" -Recurse

# ワークスペースチェック
cargo check --workspace --all-targets

# Clippy 厳格チェック
cargo clippy --workspace --all-targets -- -D warnings

# 高速並列ビルド (12コア)
.\ultra-fast-build-install.ps1 -MaxParallelJobs 12

# GUI lint
cd gui; npm run lint
```

---

## 8. 次のアクション

1. `grep` で残存 conflict markers を全て検出
2. 各ファイルの conflict を `upstream/main` ベースで解消
3. `cargo check --workspace` 成功確認
4. `cargo clippy --workspace -- -D warnings` 警告0達成
5. `gui/` で `npm run lint` 警告0達成
6. 全バイナリビルド・インストール
7. `_docs/2026-02-06_セマンティクス更新.md` に実装ログ作成

---

**戦略**: 型安全第一、ベストプラクティス遵守、完全統合を目指す
