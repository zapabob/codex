# 引き継ぎメタプロンプト: Codex プロジェクト セマンティクス更新 (v3)

## 1. コンテキスト

**プロジェクト**: Codex - AIコーディングアシスタント
**リポジトリ**: `c:\Users\downl\Desktop\codex-main`
**バージョン**: 2.13.0
**日時**: 2026-02-06 10:20 (JST)
**前回のエージェント**: Antigravity

---

## 2. 現在の状態 (Progress & Blockers)

### 2.1 解決済み / 進行中

- **Git Conflict**: 大部分は解消済み。
- **`codex-deep-research`**: `rmcp` への移行完了、ビルド成功。
- **`codex-windows-sandbox`**: ビルド成功。

### 2.2 重大なブロッカー (NEW)

- **`codex-core` モジュール重複**:
  `core/src/hooks.rs` と `core/src/hooks/mod.rs` が両方存在しており、コンパイルが停止しています。
  > [!IMPORTANT]
  > `hooks.rs` の内容を `hooks/mod.rs` に統合し、一方を削除する必要があります。

### 2.3 残存ビルドエラー (約21件)

`cargo check --workspace` で検出される主なエラー：

1. **`rmcp` 型不一致**:
   - `core/src/mcp_connection_manager.rs`: `rmcp::model` 周りの型推論またはインポートの不整合。
2. **構造体フィールドの欠落/変更**:
   - `exec/src/exec.rs`: `SandboxTransformRequest` フィールド不一致。
   - `core/src/client.rs`: `responses_websocket_enabled` フィールドが見つからない。
3. **関数シグネチャの不一致**:
   - `core/src/tasks/user_shell.rs`: `execute_user_shell_command` の引数が呼び出し側と不一致。

---

## 3. 実装計画 (Next Steps)

### 3.1 Phase 1: ブロッカー排除とコア修復

1. **`hooks` モジュールの整理**: 重複ファイルを削除・統合。
2. **`codex-core` と `codex-exec` の修復**:
   - `workspace_errors.txt` (取得済み) をベースに、`rmcp` 関連の型エラーを一つずつ解消。
   - `client.rs` 等の upstream 由来の変更と独自機能の競合を確認。

### 3.2 Phase 2: 全体ビルドと品質

1. **Workspace Check**: `cargo check --workspace --all-targets` 成功。
2. **Clippy/Fmt**: `-D warnings` でクリーンな状態にする。

### 3.3 Phase 3: GUI & 統合

1. **GUI Lint**: `cd gui; npm run lint`
2. **フルインストール**: `.\ultra-fast-build-install.ps1` で動作確認。

---

## 4. 主要ファイル / リソース

- **エラーログ**: `codex-rs/workspace_errors.txt`
- **タスク管理**: `.gemini/antigravity/brain/.../task.md`
- **前回計画 (v2)**: `_docs/2026-02-06_引き継ぎメタプロンプト_セマンティクス更新_v2.md`

---

## 5. 推奨コマンド

```powershell
# エラーの再確認
cargo check -p codex-core

# 重複ファイルの削除 (確認後に実行)
# rm codex-rs/core/src/hooks.rs
```

---

**戦略**: まず `hooks` 重複を直し、次に `codex-core` の型エラーを優先的に解決してください。
