# 2026-02-07 Build Fixes and Linter Compliance

## Summary

Fixed compilation errors in the `codex-cli` crate and addressed several linter warnings in `codex-tui` and GitHub workflows.

## Details

### 1. Fix OtelEventManager::new Signatures

Updated calls to `OtelEventManager::new` in the following files:

- `codex-rs/cli/src/agent_create_cmd.rs`
- `codex-rs/cli/src/delegate_cmd.rs`
- `codex-rs/cli/src/parallel_delegate_cmd.rs`

The signature change involved adding an `originator` string and ensuring correct argument order for telemetry data.

### 2. TUI Cleanup

- Removed unused field `recent_chunks` from `UnifiedExecProcessDetails` in `codex-rs/tui/src/history_cell/exec.rs`.

### 3. Workflow Warining Fixes

- Mapped secrets to environment variables within steps for:
  - `.github/workflows/issue-labeler.yml`
  - `.github/workflows/rust-release.yml`

This avoids the "Context access might be invalid" warning from GitHub Actions.

## Verification

- `cargo check -p codex-cli` confirmed the fix for CLI compilation.
- Dead code warning resolved in TUI.
- Workflow YAMLs updated to follow best practices.
