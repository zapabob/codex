# セマンティックバージョンアップv2.8.2とREADME・アーキテクチャ図更新

**作成日時**: 2025-12-30  
**ワークツリー**: main

---

## 📋 概要

Codexのバージョンを2.8.0から2.8.2にアップし、README.mdとCHANGELOG.mdを更新、Mermaid CLIでアーキテクチャ図を更新しました。

---

## 🔄 変更内容

### 1. バージョン番号の更新

#### Cargo.toml
- **変更前**: `version = "2.8.0"`
- **変更後**: `version = "2.8.2"`

#### package.json（主要ファイル）
- **package.json**: `"version": "2.8.0"` → `"version": "2.8.2"`
- **codex-cli/package.json**: `"version": "2.8.0"` → `"version": "2.8.2"`
- **packages/protocol-client/package.json**: `"version": "2.8.0"` → `"version": "2.8.2"`

### 2. CHANGELOG.mdの更新

v2.8.2のエントリを追加：

```markdown
## [2.8.2] - 2025-12-30

### MCP Server Startup Fixes & Feature Flag Updates

This release fixes MCP server startup errors and updates deprecated feature flags.

### Fixed

**MCP Server Configuration**
- Fixed `codex-gemini-mcp` startup error (program not found) by disabling unavailable server
- Fixed `codex-research`, `codex-agent`, `codex-supervisor` handshaking failures by disabling non-MCP subcommands
- These subcommands remain available as CLI commands (e.g., `codex research <topic>`)

**Feature Flags**
- Replaced deprecated `web_search` with `web_search_request` feature flag
- Updated configuration to use new feature flag format

### Changed

**Configuration**
- Updated `config.toml` to disable non-functional MCP servers
- Improved MCP server error handling and startup validation
```

### 3. README.mdの更新

- バージョンバッジを2.8.2に更新
- アーキテクチャ図の参照を`architecture-v2.8.2.svg`に更新
- "What's New"セクションにv2.8.2の変更点を追加

### 4. Mermaidアーキテクチャ図の更新

#### 新規ファイル作成
- **ファイル**: `docs/architecture/architecture-v2.8.2.mmd`
- **変更点**:
  - すべてのバージョン番号を2.8.2に更新
  - MCP Dynamic Loaderコンポーネントを追加
  - MCP Config Managerコンポーネントを追加
  - ConfigTOMLに`web_search_request`フラグを追加
  - MCP Integrationセクションに「v2.8.2 - Fixed」ラベルを追加

#### SVG生成
- **コマンド**: `mmdc -i docs/architecture/architecture-v2.8.2.mmd -o architecture-v2.8.2.svg -t dark -b transparent`
- **出力**: `architecture-v2.8.2.svg`（ルートディレクトリ）

---

## 📊 アーキテクチャ図の主な変更点

### 追加されたコンポーネント

1. **MCP Dynamic Loader** (Core Runtime)
   - Runtime server management
   - Token optimization
   - Selective loading

2. **MCP Config Manager** (MCP Integration)
   - Startup validation
   - Error handling
   - Feature flags

### 更新されたコンポーネント

1. **ConfigTOML** (Storage & Config)
   - `web_search_request`フラグの追加

2. **MCP Integration**セクション
   - 「v2.8.2 - Fixed」ラベルの追加
   - 起動エラーが修正されたことを明示

---

## ✅ 完了したタスク

- [x] バージョンを2.8.2に更新（Cargo.toml、package.json）
- [x] CHANGELOG.mdにv2.8.2の変更点を追加
- [x] README.mdのバージョン情報を2.8.2に更新
- [x] Mermaidアーキテクチャ図をv2.8.2に更新
- [x] Mermaid CLIでアーキテクチャ図を生成

---

## 🎯 まとめ

- バージョン2.8.2への更新を完了
- MCPサーバー起動エラー修正と機能フラグ更新を反映
- アーキテクチャ図を最新の状態に更新
- ドキュメントを完全に同期

**修正者**: Codex AI Agent  
**修正日時**: 2025-12-30
