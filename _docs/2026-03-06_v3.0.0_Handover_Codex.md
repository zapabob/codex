# v3.0.0 Handover & Implementation Instructions (Codex)

## Overview

- **Project**: Codex (zapabob expansion)
- **Version**: Official v3.0.0 Release
- **Status**: Merged to `main`. Build stabilization in progress.
- **Date**: 2026-03-06

## Specific Error Locations & Fixes (v3.0.0 Integration)

### 1. Protocol Mismatch (Protocol v2)

- **File**: [exec/src/lib.rs](file:///C:/Users/downl/Desktop/codex-main/codex-rs/exec/src/lib.rs)
- **Error**: `Op::UserTurn` and `Op::ResolveElicitation` missing fields.
- **Fix**: Added `service_tier: None` and `content: None` respectively.
- **Context**: Upstream v3.0.0 introduced fine-grained service tiers and content metadata.

### 2. Main Entry Point Type Mismatch

- **File**: [exec/src/main.rs](file:///C:/Users/downl/Desktop/codex-main/codex-rs/exec/src/main.rs)
- **Error**: `run_main(inner, arg0_paths)` expected `Option<PathBuf>`, found `Arg0DispatchPaths`.
- **Fix**: Changed to `run_main(inner, arg0_paths.codex_linux_sandbox_exe)`.

### 3. Lifetime Errors in Async Closures

- **File**: [app-server/src/codex_message_processor.rs](file:///C:/Users/downl/Desktop/codex-main/codex-rs/app-server/src/codex_message_processor.rs)
- **Error**: Borrowed value does not live long enough in `spawn_blocking`.
- **Fix**: Cloned the attachment path to an owned `PathBuf` before the move.

### 4. Dead Code / Unused Constants

- **File**: [app-server/src/main.rs](file:///C:/Users/downl/Desktop/codex-main/codex-rs/app-server/src/main.rs)
- **Error**: `MANAGED_CONFIG_PATH_ENV_VAR` warning in release.
- **Fix**: Wrapped in `#[cfg(debug_assertions)]`.

## Key Changes in v3.0.0

### 1. Protocol Compatibility

- **`exec/src/lib.rs`**: Updated `Op::UserTurn` and `Op::ResolveElicitation` to match the new v3.0.0 protocol requirements (added `service_tier` and `content` optional fields).
- **Core Engine**: Maintained compatibility with `codex_protocol::protocol` re-exports.

### 2. Build Regressions Resolved

- **`exec/src/main.rs`**: Resolved type mismatch in `run_main` by correctly passing `arg0_paths.codex_linux_sandbox_exe`.
- **`app-server/src/main.rs`**: Wrapped `MANAGED_CONFIG_PATH_ENV_VAR` in `#[cfg(debug_assertions)]` to eliminate unused constant warnings in release builds.
- **`app-server/src/codex_message_processor.rs`**: Fixed a lifetime issue in `spawn_blocking` closure by properly cloning attachment paths.

### 3. Documentation & Branding

- **`README.md`**: Overhauled with "Killer Features" focus (Multi-agent, Zero-Trust Sandbox, Git4D, Deep Research). Updated badges to v3.0.0, Next.js 15, and MCP 1.1.

## Pending Error Locations (Error Inheritance)

### 1. `cli` Crate: Plan Component Mismatch

- **File**: [cli/src/plan_commands.rs](file:///C:/Users/downl/Desktop/codex-main/codex-rs/cli/src/plan_commands.rs)
- **Error**: `PlanBlock` and `EvalCriteria` fields mismatched with new `codex_core::plan` definition.
- **Status**: Fails during `cargo check --workspace`.
- **Action**: Align `PlanBlock` initialization in `create_plan` with the updated struct in `core`.

### 2. `cli` Crate: QC Command Errors

- **File**: [cli/src/qc_cmd.rs](file:///C:/Users/downl/Desktop/codex-main/codex-rs/cli/src/qc_cmd.rs)
- **Error**: Type and import errors in `run_qc_command`.
- **Status**: Regression post-sync.
- **Action**: Fix imports and type alignment for `QcAgent` and `QcConfig`.

### 3. Missing Binaries in Target

- **Issue**: `codex.exe` occasionally missing from `target/release` during partial builds.
- **Action**: Run `cargo build --release -p codex-cli` explicitly after cleaning workspace locks.

## Prioritized Action Items (Owner Notes)

### 1. Rust Build & Installation (Highest Priority)

- [ ] **Finish Release Build**: Let the current workspace build finish using `CARGO_TARGET_DIR=target-release`.
- [ ] **Rebuild CLI**: Ensure `codex-cli` is built with `--features custom-features`.
- [ ] **Gated Install**: Do NOT copy to `bin\` until `target-release\release\codex.exe` exists and `codex --version` reports `3.0.0`.

### 2. GUI Stabilization (Critical)

- [ ] **Fix Crash Cluster**: Remove manual Three.js chunk splitting from `gui/next.config.mjs`. This fixes the `TypeError: Cannot read properties of undefined (reading 'ReactCurrentBatchConfig')` on `/visualization`, `/git4d`, and `/vr`.
- [ ] **Version Pinning**: Pin React to `18.3.1` in `gui/package.json` to stabilize the release branch.
- [ ] **VR Provider**: Fix `VRInterface` to render within an XR provider or gate it.

### 3. Cleanup & Verification

- [ ] **WebSocket derive**: Update `gui/src/lib/api/client.ts` to derive URL from `baseUrl`.
- [ ] **Standalone Retest**: Validate using `node .next\standalone\gui\server.js` on port `3002`.

## Verified Fixed (Ready)

- Protocol v3 `Op::UserTurn`/`Op::ResolveElicitation` fields.
- `exec` type mismatches and `arg0_paths` handling.
- `app-server` async lifetime errors and dead-code warnings.
- `sysinfo` API and `model_reasoning_summary` logic.

## Do Not Do

- Do not overwrite `bin\codex.exe` before `target-release\release\codex.exe` exists and passes smoke tests.
- Do not kill every process named `codex.exe` indiscriminately (MCP servers may be active).
- Do not trust older handover notes without checking against the latest source.

---

_Reference: [\_docs/2026-03-06_v3.0.0_Antigravity_Implementation_Instructions.md](file:///c:/Users/downl/Desktop/codex-main/_docs/2026-03-06_v3.0.0_Antigravity_Implementation_Instructions.md)_
