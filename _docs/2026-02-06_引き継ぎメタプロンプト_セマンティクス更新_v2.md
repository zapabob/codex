# 引き継ぎメタプロンプト: Codex プロジェクト セマンティクス更新 (v2)

## 1. コンテキスト

**プロジェクト**: Codex - AIコーディングアシスタント (OpenAI Codex CLIの拡張フォーク)
**リポジトリ**: `c:\Users\downl\Desktop\codex-main`
**バージョン**: 2.13.0
**日時**: 2026-02-06 09:35 (JST)
**前回のエージェント**: Antigravity

---

## 2. 現在の状態

### 2.1 完了した作業

- **Git Conflict markers の一括解消**: Pythonスクリプトにより約18ファイルのマージ競合を解消済み。
- **`codex-deep-research`**: `mcp-types`から`rmcp`への型移行に伴う修正完了。ビルド成功。
- **`codex-app-server-protocol`**: `generate_ts_with_options`の追加とprettier周りの修正完了。
- **`codex-windows-sandbox`**: ビルド成功。

### 2.2 現在のブロッカー (未解決のビルドエラー)

`cargo check --workspace` で **21件のエラー** が残っています。主な原因は以下の通りです：

1.  **`codex-exec` 関連**:
    - `mcp_connection_manager.rs`: `rmcp` クレートへの移行に伴う型不明エラー (`use rmcp::model::Content` などの修正が必要)。
    - `user_shell.rs`: `UserShellCommandMode` や `execute_user_shell_command` 関数の引数不一致。
    - `exec.rs`: `SandboxTransformRequest` 構造体フィールドの不一致。

2.  **`codex-core` 関連**:
    - `client.rs`: `CodexClient` 構造体のフィールドやメソッドが見つからない (`responses_websocket_enabled` など)。
    - `codex.rs`: `run_model_turn` の引数不一致。

詳細なエラーログは `workspace_errors.txt` に保存されています。

---

## 3. 実装計画 (Next Steps)

### 3.1 Phase 1: 残存コンパイルエラーの解消 (最優先)

以下の順序でエラーを修正してください：

1.  **`workspace_errors.txt` の分析**:
    - エラーの内容を確認し、`rmcp` への移行漏れや、upstream との統合不整合を特定する。
2.  **`codex-exec`, `codex-core` の修正**:
    - 特に `client.rs`, `exec.rs` は upstream の変更を取り込みつつ、独自機能（Windows Sandbox対応など）を維持すること。
3.  **ビルド確認**:
    ```powershell
    cargo check --workspace --all-targets
    ```

### 3.2 Phase 2: 品質向上 (Rust 2024 & Lint)

1.  **Clippy 警告の解消**:
    ```powershell
    cargo clippy --workspace -- -D warnings
    ```
2.  **Formatter**:
    ```powershell
    cargo fmt
    ```

### 3.3 Phase 3: GUI & 統合ビルド

1.  **GUI Lint**:
    ```powershell
    cd gui; npm run lint
    ```
2.  **全バイナリビルド**:
    ```powershell
    .\ultra-fast-build-install.ps1
    ```

---

## 4. 品質基準 (必須達成)

| 項目         | 目標               |
| ------------ | ------------------ |
| 型定義エラー | 0                  |
| Clippy 警告  | 0                  |
| ESLint 警告  | 0                  |
| ビルド成功   | 全バイナリ正常生成 |

---

## 5. 現在のアーティファクト

- **タスクリスト**: `.gemini/antigravity/brain/.../task.md`
- **エラーログ**: `codex-rs/workspace_errors.txt`
- **コンフリクト解消スクリプト**: `codex-rs/resolve_conflicts.py`

---

## 6. すぐに実行すべきコマンド

```powershell
# エラーログの確認 (再取得推奨)
cargo check --workspace --all-targets

# 特定パッケージのチェック (例)
cargo check -p codex-core
```
