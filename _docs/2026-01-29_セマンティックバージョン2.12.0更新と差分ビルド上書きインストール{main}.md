## 実装ログ（2026-01-29）

### 目的
- プロジェクトのセマンティックバージョンを **2.12.0** に更新
- **高速差分ビルド（incremental）**で `codex` バイナリを生成
- バイナリを **コピー＆ペースト上書き（Copy-Item -Force）**でインストール

### 変更点
- `codex-rs/Cargo.toml`
  - `[workspace.package] version = "2.12.0"`

### ビルド＆インストール手順
- `scripts/clean-build-install.ps1 -SkipClean -InstallPath "$env:USERPROFILE\.cargo\bin\codex.exe"`
  - `-SkipClean` により差分ビルド（incremental）を優先
  - 実行中の `codex` / `cargo` / `rustc` を停止してロック回避
  - `target\release\codex.exe` をインストール先へ `Copy-Item -Force` で上書き

