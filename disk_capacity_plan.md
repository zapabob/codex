# Cドライブ容量確保計画 - 緊急対応ガイド

**作成日時**: 2025-12-30 17:55:45
**状況**: Cドライブ容量が完全に不足（ENOSPCエラー発生中）
**推奨アクション**: 即時対応が必要

## 🚨 現在の状況

- Cドライブの空き容量が0%に近づいている
- 通常のファイル操作・コマンド実行が不可能
- システムが不安定な状態

## 🛠️ 緊急対応策

### 方法1: セーフモードでの起動（推奨）

1. **PCを再起動**
2. **Shiftキーを押しながら再起動**（または設定から）
3. **トラブルシューティング > 詳細オプション > スタートアップ設定 > 再起動**
4. **セーフモード（ネットワーク）**を選択

**セーフモードで実行できること**:
- 不要ファイルの一括削除
- ディスククリーンアップ
- 一時ファイルの削除

### 方法2: 外部メディアからの起動

**必要なもの**: USBブートドライブ、インストールメディア

1. **WindowsインストールUSBを作成**
2. **BIOS/UEFI設定でUSBブートを優先**
3. **コマンドプロンプトから容量確保**

### 方法3: 別のドライブへのファイル移動

**前提**: Dドライブなど別のドライブが存在する場合

```batch
# セーフモードまたは外部メディアから実行
robocopy "C:\Users\downl\Desktop" "D:\Backup\Desktop" /MIR /R:1 /W:1
rd /s /q "C:\Users\downl\Desktop"
```

## 📋 容量確保対象の優先順位

### 高優先度（安全に削除可能）

1. **一時ファイル**
   - `C:\Windows\Temp\*`
   - `C:\Users\[User]\AppData\Local\Temp\*`
   - `%TEMP%\*`

2. **ごみ箱**
   - `C:\$Recycle.Bin\*` （既に空の場合あり）

3. **ダウンロードフォルダ**
   - `C:\Users\[User]\Downloads\*`

4. **ブラウザキャッシュ**
   - Chrome: `C:\Users\[User]\AppData\Local\Google\Chrome\User Data\Default\Cache\*`
   - Edge: `C:\Users\[User]\AppData\Local\Microsoft\Edge\User Data\Default\Cache\*`

### 中優先度（確認が必要）

5. **古いWindows更新ファイル**
   - `C:\Windows\WinSxS\*` の古いバージョン

6. **システムログ**
   - `C:\Windows\Logs\*`
   - `C:\Windows\Debug\*`

7. **プログラムの一時ファイル**
   - `C:\ProgramData\Package Cache\*`

### 低優先度（注意が必要）

8. **ユーザー文書・メディア**
   - `C:\Users\[User]\Documents\*`
   - `C:\Users\[User]\Pictures\*`
   - `C:\Users\[User]\Videos\*`

## ⚡ 即時実行コマンド（セーフモードから）

```batch
@echo off
echo Cドライブ容量確保スクリプト

REM 一時ファイル削除
del /f /s /q "%TEMP%\*" 2>nul
rd /s /q "%TEMP%" 2>nul
md "%TEMP%"

REM Windows一時ファイル削除
del /f /s /q "C:\Windows\Temp\*" 2>nul
rd /s /q "C:\Windows\Temp" 2>nul
md "C:\Windows\Temp"

REM ごみ箱削除（念のため）
rd /s /q "C:\$Recycle.Bin" 2>nul
md "C:\$Recycle.Bin"

REM 容量確認
echo 容量確認:
dir /-c C:\ | find "bytes free"
pause
```

## 🔧 システム設定変更

### ページファイルサイズの一時縮小

1. **ファイル名を指定して実行** → `sysdm.cpl`
2. **詳細設定** → **パフォーマンス設定**
3. **詳細**タブ → **仮想メモリ**変更
4. **自動サイズ管理を無効化**
5. **初期サイズ・最大サイズを一時的に縮小**（例: 1024MB）

### 不要サービスの停止

**セーフモードで実行可能**:
- Windows Search
- Windows Defender（一時的に）
- Superfetch/SysMain

## 📊 期待される容量確保量

| 対象 | 予想容量 | 難易度 |
|------|----------|--------|
| 一時ファイル | 1-5GB | 簡単 |
| ごみ箱 | 0-82GB | 簡単 |
| ダウンロード | 1-10GB | 簡単 |
| ブラウザキャッシュ | 0.5-2GB | 簡単 |
| Windows更新 | 2-10GB | 中 |
| システムログ | 0.1-1GB | 簡単 |
| **合計予想** | **10-100GB** | - |

## 🚨 注意事項

### ⚠️ 削除してはいけないもの

- ❌ `C:\Windows\System32\*` （システムファイル）
- ❌ `C:\Program Files\*` （プログラムファイル）
- ❌ `C:\Users\[User]\NTUSER.DAT` （ユーザー設定）
- ❌ 現在の作業中ファイル

### 🔄 バックアップ推奨

- 重要な文書は事前にバックアップ
- システムイメージ作成（可能であれば）

### 📞 緊急時の対応

1. **容量不足が深刻な場合**: 外部HDD/SSDを接続してファイルを移動
2. **システムが起動しない場合**: インストールメディアから修復モード
3. **データ復元が必要な場合**: 専門ツールの使用（別PCから）

## 🎯 次のステップ

1. **セーフモードで起動**
2. **上記スクリプトを実行**
3. **容量を確認**
4. **必要に応じて追加削除**
5. **通常モードで再起動**

---

**緊急対応のため、この計画はセーフモードでの実行を前提としています。**
**不明点があれば、専門家に相談してください。**
