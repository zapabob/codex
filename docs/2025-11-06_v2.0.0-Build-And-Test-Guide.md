# Codex v2.0.0 - ビルド・テスト・インストールガイド

**作成日**: 2025-11-06 05:20  
**対象**: 開発者・実機テスト実施者  
**バージョン**: v2.0.0  

---

## 🎯 目的

Codex v2.0.0をCUDA機能付きでビルドし、実機でテストするための完全ガイド。

---

## 📋 前提条件

### システム要件

**必須**:
- OS: Windows 10/11, macOS 10.15+, Linux (Ubuntu 20.04+)
- Rust: 1.75+ (2024 edition)
- Git: 2.30+

**CUDA機能用（オプション）**:
- NVIDIA GPU (Compute Capability 3.5+)
- CUDA Toolkit 11.0+ ([ダウンロード](https://developer.nvidia.com/cuda-toolkit))
- Windows: Visual Studio 2019+ (CUDA用)

**Windows AI機能用（オプション）**:
- Windows 11 Build 26100+ (25H2)
- DirectX 12対応GPU
- AI Kernel Driver (オプション、最大性能用)

### 環境変数設定

#### Step 1: env.sampleをコピー

```bash
# Windows PowerShell
Copy-Item env.sample .env

# Linux/macOS
cp env.sample .env
```

#### Step 2: .envを編集

```bash
# 必須: OpenAI API Key
OPENAI_API_KEY=sk-proj-your-actual-key-here

# オプション: GitHub Token
GITHUB_TOKEN=ghp_your_token_here

# オプション: CUDA Device
CUDA_DEVICE=0
```

#### Step 3: 環境変数読み込み

```powershell
# Windows PowerShell
Get-Content .env | ForEach-Object {
    if ($_ -match '^([^#][^=]+)=(.*)$') {
        [Environment]::SetEnvironmentVariable($matches[1], $matches[2], "Process")
    }
}
```

```bash
# Linux/macOS
export $(cat .env | grep -v '^#' | xargs)
```

---

## 🔧 ビルド手順

### ディスク容量確保

```bash
cd codex-rs

# 古いビルド成果物削除（約2-3GB確保）
cargo clean
```

### Feature別ビルド

#### 1. デフォルトビルド（GPU機能なし）

```bash
cargo build --release -p codex-cli
```

**ビルド時間**: 約5-10分（初回）  
**成果物**: `target/release/codex.exe` (Windows) / `target/release/codex` (Unix)

#### 2. CUDA機能付きビルド

**前提**: CUDA Toolkit インストール済み

```bash
# CUDA機能のみ
cargo build --release --features cuda -p codex-cli

# すべてのGPU機能
cargo build --release --features "cuda,windows-ai" -p codex-cli
```

**ビルド時間**: 約10-15分（初回）  
**ディスク使用**: 約3-4GB

#### 3. 全コンポーネントビルド

```bash
# すべて（CLI, TUI, GUI）
cargo build --release --workspace --features "cuda,windows-ai"
```

**ビルド時間**: 約20-30分  
**ディスク使用**: 約5-6GB

### ビルドエラー対処

#### エラー: "ディスクに十分な空き領域がありません"

**原因**: ビルドキャッシュ肥大化

**解決**:
```bash
# 完全クリーン
cargo clean

# targetディレクトリサイズ確認
du -sh target/  # Unix
Get-ChildItem target -Recurse | Measure-Object -Property Length -Sum  # Windows
```

#### エラー: "CUDA not found"

**原因**: CUDA Toolkit未インストール

**解決**:
```bash
# CUDA確認
nvcc --version

# インストール必要な場合
# https://developer.nvidia.com/cuda-toolkit
```

#### エラー: "feature 'cuda' not found"

**原因**: Cargo.tomlにfeature定義がない

**解決**: すでに実装済み（v2.0.0で追加）

---

## 📦 インストール

### 上書きインストール

```bash
cd codex-rs

# CUDA機能付きインストール
cargo install --path cli --features cuda --force

# すべてのGPU機能付き
cargo install --path cli --features "cuda,windows-ai" --force
```

**--forceフラグ**: 既存インストールを上書き

### インストール確認

```bash
# バージョン確認
codex --version
# → codex-cli 2.0.0

# CUDA利用可能確認
codex --use-cuda --version

# Windows AI利用可能確認（Windows 11 25H2+のみ）
codex --use-windows-ai --version
```

---

## 🧪 実機テスト

### テスト1: 基本動作確認

```bash
# ヘルプ表示
codex --help

# バージョン確認
codex --version
# 期待: codex-cli 2.0.0
```

### テスト2: CUDA機能テスト

```bash
# CUDAデバイス情報確認
nvidia-smi

# CUDA加速テスト
codex --use-cuda "List files in current directory"
```

**期待出力**:
```
⚡ CUDA acceleration: ENABLED
🚀 Using GPU: NVIDIA GeForce RTX 3080
```

### テスト3: Git解析パフォーマンステスト

```bash
# CPU版（ベースライン）
time codex git-analyze commits --limit 1000

# CUDA版（100倍高速）
time codex git-analyze commits --use-cuda --limit 1000
```

**期待結果**:
- CPU: 約0.5秒
- CUDA: 約0.005秒（100倍高速）

### テスト4: 3D可視化テスト

```bash
# 3D可視化起動
codex git-analyze visualize-3d --use-cuda

# JSON export
codex git-analyze visualize-3d --use-cuda --export-json commits-3d.json
```

**期待出力**:
```
🚀 Launching 3D Git Visualization (Kamui4D-exceeding)
⚡ CUDA acceleration: ENABLED
✅ Analyzed 10000 commits
📊 Statistics:
  Total commits: 10000
💡 Tip: Use --export-json to save 3D data
```

### テスト5: Windows AI統合テスト（Windows 11 25H2+のみ）

```bash
# Windows AI テスト
codex --use-windows-ai "Analyze this codebase"

# Kernel Driver テスト
codex --use-windows-ai --kernel-accelerated "Complex task"

# Hybrid（両方）
codex --use-windows-ai --use-cuda "Maximum performance task"
```

**期待出力**:
```
🚀 Windows AI enabled (kernel_accelerated: true)
⚡ CUDA acceleration: ENABLED
🔥 Hybrid mode: Automatic selection
```

---

## 📊 パフォーマンスベンチマーク

### ベンチマーク実行

```bash
cd codex-rs/cuda-runtime

# ベンチマーク実行（CUDA vs CPU比較）
cargo bench --features cuda
```

**測定指標**:
1. Git解析速度（1,000 / 10,000 / 100,000コミット）
2. 3D座標計算時間
3. メモリ使用量
4. GPU利用率

### 結果の見方

```
test git_analysis_cpu_1k  ... bench:   500,000 ns/iter (+/- 50,000)
test git_analysis_cuda_1k ... bench:     5,000 ns/iter (+/- 500)
```

**解釈**: CUDA版が100倍高速 ✅

---

## 🔍 トラブルシューティング

### 問題1: CUDAビルドが失敗

**症状**:
```
error: failed to run custom build command for `cust`
```

**解決**:
1. CUDA Toolkitインストール確認
```bash
nvcc --version
# 期待: release 11.0 以上
```

2. 環境変数確認
```bash
echo $CUDA_HOME  # Linux/macOS
echo %CUDA_PATH%  # Windows
```

3. Visual Studio確認（Windows）
```bash
# VS 2019/2022 インストール済みか確認
cl.exe
```

### 問題2: インストール後にCUDAが使えない

**症状**:
```
⚠️  CUDA not available (compile with --features cuda)
```

**原因**: CUDA機能なしでビルド

**解決**:
```bash
# 再インストール（CUDA付き）
cargo install --path cli --features cuda --force
```

### 問題3: Git解析が遅い

**症状**: CUDA版でも遅い

**確認**:
```bash
# GPUが認識されているか
nvidia-smi

# CUDA runtime確認
codex --use-cuda "test"
```

**解決**:
- GPUドライバー更新
- CUDA Toolkitバージョン確認
- `--use-cuda` フラグ付け忘れ確認

### 問題4: Windows AIが利用できない

**症状**:
```
⚠️  Windows AI requested but not available
```

**確認**:
```bash
# Windows build確認
winver
# 期待: Build 26100以上
```

**解決**:
- Windows Update実行
- Windows 11 25H2にアップグレード

---

## 📝 テスト結果記録テンプレート

### テスト環境

```
OS: Windows 11 Build 26100
CPU: [Your CPU]
GPU: NVIDIA GeForce RTX 3080
RAM: [Your RAM]
CUDA: 12.0
Codex: 2.0.0
```

### テスト結果

| テスト | 結果 | 所要時間 | 備考 |
|--------|------|---------|------|
| 基本動作 | ✅ / ❌ | - | - |
| CUDA機能 | ✅ / ❌ | - | - |
| Git解析（CPU） | ✅ / ❌ | Xs | - |
| Git解析（CUDA） | ✅ / ❌ | Xs | - |
| 3D可視化 | ✅ / ❌ | - | - |
| Windows AI | ✅ / ❌ | - | - |

### パフォーマンス測定

| 操作 | CPU | CUDA | 高速化率 |
|------|-----|------|---------|
| git解析（1k） | Xs | Xs | Xx |
| git解析（10k） | Xs | Xs | Xx |
| 推論 | Xms | Xms | Xx |

---

## 🚀 クイックスタート

### 最小手順（CUDA有効）

```bash
# 1. ディレクトリ移動
cd codex-rs

# 2. クリーン
cargo clean

# 3. ビルド（CUDA付き）
cargo build --release --features cuda -p codex-cli

# 4. インストール
cargo install --path cli --features cuda --force

# 5. 確認
codex --version

# 6. テスト
codex --use-cuda "Hello CUDA!"
codex git-analyze commits --use-cuda --limit 1000
```

---

## 📊 期待されるパフォーマンス

### ベンチマーク目標（v2.0.0）

| 操作 | 目標 | 実測 | ステータス |
|------|------|------|-----------|
| Git解析（10k、CUDA） | < 0.1秒 | ___ | ⏳ |
| 推論（CUDA） | < 5ms | ___ | ⏳ |
| 3D描画 | 120fps | ___ | ⏳ |
| GPU利用率 | > 90% | ___ | ⏳ |

### Kamui4Dとの比較

| 指標 | Kamui4D | Codex目標 | 実測 | 達成 |
|------|---------|-----------|------|------|
| 解析（10k） | 5秒 | 0.05秒 | ___ | ⏳ |
| FPS | 60fps | 120fps | ___ | ⏳ |
| 最大コミット | 1,000 | 100,000 | ___ | ⏳ |

---

## ✅ チェックリスト

### ビルド前
- [ ] CUDA Toolkit インストール確認
- [ ] ディスク空き容量確認（5GB以上）
- [ ] 環境変数設定（.env）
- [ ] Rust toolchain更新

### ビルド
- [ ] cargo clean実行
- [ ] CUDA feature付きビルド成功
- [ ] バイナリサイズ確認（< 100MB）
- [ ] 依存クレート確認

### インストール
- [ ] 上書きインストール成功
- [ ] バージョン確認（2.0.0）
- [ ] PATH設定確認
- [ ] 実行権限確認

### テスト
- [ ] 基本動作確認
- [ ] CUDA機能確認
- [ ] Git解析パフォーマンス測定
- [ ] 3D可視化動作確認
- [ ] Windows AI統合確認（該当環境のみ）

### 結果記録
- [ ] パフォーマンスデータ記録
- [ ] エラー・警告記録
- [ ] フィードバック作成
- [ ] Issue報告（必要に応じて）

---

## 🎓 参考情報

### ビルドコマンド一覧

```bash
# 最小ビルド
cargo build --release -p codex-cli

# CUDA付き
cargo build --release --features cuda -p codex-cli

# Windows AI付き
cargo build --release --features windows-ai -p codex-cli

# 全機能
cargo build --release --features "cuda,windows-ai" -p codex-cli

# 全コンポーネント
cargo build --release --workspace --all-features
```

### 実行コマンド一覧

```bash
# 基本
codex "Your task"

# CUDA
codex --use-cuda "Your task"

# Windows AI
codex --use-windows-ai "Your task"

# Hybrid
codex --use-windows-ai --use-cuda "Your task"

# Git解析
codex git-analyze commits --use-cuda --limit 10000
codex git-analyze visualize-3d --use-cuda
codex git-analyze heatmap --limit 10000

# MCP経由CUDA
# （MCPサーバー経由で自動的に使用）
```

---

## 📝 実機テストレポートフォーマット

```markdown
# Codex v2.0.0 実機テスト結果

## 環境
- OS: Windows 11 Build 26100
- CPU: Intel Core i9-12900K
- GPU: NVIDIA GeForce RTX 3080
- RAM: 32GB
- CUDA: 12.0
- Codex: 2.0.0

## ビルド
- ビルド時間: 12分
- 成果物サイズ: 85MB
- Features: cuda
- 警告: 0

## パフォーマンステスト

### Git解析（10,000コミット）
- CPU: 4.8秒
- CUDA: 0.048秒
- 高速化: 100倍 ✅

### 推論レイテンシ
- CPU: 9.5ms
- CUDA: 2.1ms
- 高速化: 4.5倍 ✅

### 3D可視化
- FPS: 118fps
- GPU利用率: 92%
- メモリ使用: 8GB

## Kamui4D比較
- 解析速度: ✅ 100倍高速
- FPS: ✅ 2倍
- スケール: ✅ 100倍（100,000コミット対応）

## 総評
すべての目標達成！Kamui4D完全超越確認！🏆
```

---

**作成日**: 2025-11-06 05:20  
**対象バージョン**: v2.0.0  
**ステータス**: 📝 **ビルド・テストガイド完成**

**次のステップ**: 実機テスト実施 → 結果記録 → フィードバック


























