# セーフモードでのCドライブ容量確保ガイド

## 🚨 緊急対応: セーフモード起動方法

### 方法1: Windows 11でのセーフモード起動

#### ステップ1: スタートメニューから
1. **スタートボタンを右クリック** → **電源** → **Shiftキーを押しながら再起動**
2. または: **設定** → **システム** → **回復** → **今すぐ再起動**

#### ステップ2: トラブルシューティング選択
- **トラブルシューティング** を選択
- **詳細オプション** を選択
- **スタートアップ設定** を選択
- **再起動** をクリック

#### ステップ3: セーフモード選択
- PCが再起動したら、番号キーで選択:
  - **4** または **F4**: セーフモード
  - **5** または **F5**: セーフモード（ネットワーク）

### 方法2: Windows 10でのセーフモード起動

#### ステップ1: Shift + 再起動
1. **スタートボタン** をクリック
2. **電源アイコン** をクリック
3. **Shiftキーを押しながら** → **再起動** をクリック

#### ステップ2: トラブルシューティング
- **トラブルシューティング** → **詳細オプション** → **スタートアップ設定** → **再起動**

#### ステップ3: セーフモード
- **4** を押してセーフモードを選択

## 🔧 セーフモードでの容量確保実行

### ステップ1: スクリプトの実行場所を確認
- 作成された `cleanup_script.bat` が `C:\Users\downl\Desktop\codex-main\` にあります

### ステップ2: コマンドプロンプトを開く
1. **Windowsキー + R** を押す
2. `cmd` と入力してEnter
3. または: スタートメニュー → Windowsシステムツール → コマンドプロンプト

### ステップ3: スクリプトを実行
```cmd
cd C:\Users\downl\Desktop\codex-main
cleanup_script.bat
```

### ステップ4: 実行内容の確認
スクリプトは以下の処理を自動実行します：
- ✅ 一時ファイル削除（1-5GB）
- ✅ ごみ箱削除（0-82GB）
- ✅ ダウンロードフォルダの古いファイル削除（1-10GB）
- ✅ ブラウザキャッシュ削除（0.5-2GB）
- ✅ サムネイルキャッシュ削除
- ✅ ログファイル削除
- ✅ Windows配信最適化ファイル削除

## 📊 期待される容量確保量

| 対象項目 | 予想容量 | 削除対象 |
|----------|----------|----------|
| Windows一時ファイル | 1-2GB | `C:\Windows\Temp\*` |
| ユーザー一時ファイル | 1-3GB | `C:\Users\*\AppData\Local\Temp\*` |
| ごみ箱 | 0-82GB | `C:\$Recycle.Bin\*` |
| ダウンロード（7日以上前） | 1-10GB | `Downloads\*` (7日以上前) |
| Chromeキャッシュ | 0.5-1GB | `Chrome\User Data\Default\Cache\*` |
| Edgeキャッシュ | 0.2-0.5GB | `Edge\User Data\Default\Cache\*` |
| Firefoxキャッシュ | 0.1-0.3GB | `Firefox\Profiles\*\cache2\*` |
| システムログ | 0.1-0.5GB | `C:\Windows\Logs\*` |
| サムネイルキャッシュ | 0.1-0.2GB | `thumbcache_*.db` |
| **合計予想** | **10-100GB以上** | - |

## 🔄 セーフモードから通常モードへの復帰

### 方法1: 通常再起動
1. **スタートボタン** → **電源** → **再起動**
2. セーフモード選択画面が表示されたら、何も押さずに待つ

### 方法2: 設定変更
1. **システム構成**を開く（`msconfig`）
2. **ブート**タブ → **セーフブート**のチェックを外す
3. **OK** → **再起動**

## ⚠️ セーフモードでの注意事項

### ✅ できること
- ファイルの削除・移動
- システム設定の変更
- ディスククリーンアップ
- コマンドプロンプトの使用

### ❌ できないこと（通常時のみ）
- 一部のプログラムの実行
- インターネット接続（セーフモードの場合）
- グラフィック関連の設定変更

### 🔒 セキュリティ面
- セーフモードではウイルス対策ソフトが制限される場合がある
- 重要な操作は信頼できるファイルのみ実行

## 📞 問題が発生した場合

### スクリプトが実行できない場合
```cmd
REM 手動で実行する場合
del /f /s /q "C:\Windows\Temp\*"
del /f /s /q "%TEMP%\*"
rd /s /q "C:\$Recycle.Bin"
```

### 容量がまだ不足する場合
1. **別のドライブにファイルを移動**
2. **外部USBドライブを使用**
3. **クラウドストレージにアップロード**

### システムが起動しない場合
1. **インストールメディアから起動**
2. **コマンドプロンプトから修復**
3. **システム復元を使用**

## 🎯 成功の確認方法

### 容量確認コマンド
```cmd
dir /-c C:\
```
または
```cmd
fsutil volume diskfree c:
```

### 期待される結果
- 空き容量が10GB以上増加
- システムが正常に動作
- ファイル操作が可能になる

---

## 📝 実行後のレポート

セーフモードでの作業完了後、以下の情報を記録してください：

- 削除前の空き容量: ____ GB
- 削除後の空き容量: ____ GB
- 確保された容量: ____ GB
- 問題が発生した項目: ________
- その他の所見: ________

このガイドに従って容量確保を実行してください！